# SHOUT
## Now waaaaaaaaaaaaaaaaaaaaaaaaaait a minute

Dev mode:
```shell
$ npm install
$ cd client
$ npm install
$ cd ..
$ npm run dev
```

Prod build:
```shell
$ npm install
$ npm run build
$ NODE_ENV=production npm start
```

